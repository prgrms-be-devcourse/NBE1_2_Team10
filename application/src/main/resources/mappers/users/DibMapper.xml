<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="core.application.users.mapper.DibMapper">
    <insert id="saveNewDib"></insert>
    <delete id="deleteDibByDibId"></delete>
    <delete id="deleteDibByUserId"></delete>
    <select id="findByDibId" resultType="core.application.users.models.entities.DibEntity"></select>
    <select id="countMovie" resultType="java.lang.Long"></select>
    <select id="selectAll" resultType="core.application.users.models.entities.DibEntity"></select>
</mapper>